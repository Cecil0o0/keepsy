{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-engine",
      "type": "shell",
      "command": "zig build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build"
    }
  ]
}